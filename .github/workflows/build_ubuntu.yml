name: Build Ubuntu
on:
  push:
    branches:
      - main

  pull_request:

jobs:
  job_build_ubuntu:
    runs-on: ubuntu-latest
    steps:
    - id: CHECKOUT_SOURCE
      uses: actions/checkout@v3
    - id: MAKE
      run: make
    - id: TEST
      run: make test
